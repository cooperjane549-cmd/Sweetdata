{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user balance, ad stats, and referral info.",
      "type": "object",
      "properties": {
        "balance": { "type": "number", "description": "Current MB balance" },
        "ads_watched": { "type": "number", "description": "Total ads watched" },
        "trust_pack_claimed": { "type": "boolean", "description": "Whether the 15MB trust pack was claimed" },
        "referral_code": { "type": "string", "description": "Unique referral code" },
        "referred_by": { "type": "string", "description": "UID of the user who referred this user" }
      },
      "required": ["balance", "ads_watched", "trust_pack_claimed", "referral_code"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "UserProfile",
      "description": "User data stored in Realtime Database (mapped here for blueprinting)"
    }
  }
}
